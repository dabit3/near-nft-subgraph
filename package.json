{
  "name": "nearone",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dabit3/nearone",
    "create-local": "graph create --node http://localhost:8020/ dabit3/nearone",
    "remove-local": "graph remove --node http://localhost:8020/ dabit3/nearone",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dabit3/nearone"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
